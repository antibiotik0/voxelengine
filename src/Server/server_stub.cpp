// =============================================================================
// VOXEL ENGINE - SERVER MODULE STUB
// This file exists to satisfy CMake requirements for STATIC library
// Will be replaced with actual implementation files in Phase 1
// =============================================================================

#include "Shared/Types.hpp"
#include "Server/TickManager.hpp"

namespace voxel::server {

// Compilation validation - ensures headers are correctly formed
static_assert(sizeof(voxel::Voxel) == 4, "Voxel must be exactly 32 bits");

} // namespace voxel::server
